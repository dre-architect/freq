# =============================================================================
# FREQ AI — Terraform Variables (copy to terraform.tfvars and fill in)
# =============================================================================

project_id      = "your-gcp-project-id"
region          = "us-central1"
environment     = "production"
min_instances   = 1
max_instances   = 5
gpu_type        = "nvidia-tesla-t4"
content_version = "v1"
