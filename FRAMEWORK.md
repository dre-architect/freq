# Framework Choice

**Option C — Vanilla HTML/CSS/JS**

Zero build risk. Maximum control. No package.json needed. No npm install. No build step.
Vercel serves static HTML directly. Every page is a standalone .html file.

This is the framework that ships with zero errors.
